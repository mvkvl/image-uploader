spring:
  application:
    name: upload-service
  http:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      
server:
  port: 7070
  
logging:
  file: upload-service.log
  pattern:
    console: "%d %-5level [%thread] %logger : %msg%n"
    file:    "%d %-5level [%thread] %logger : %msg%n"    
  level:
    console: 'DEBUG'
    root: ERROR
    org.springframework.web: ERROR
    org.hibernate: ERROR
    ws.slink.test: DEBUG

images:
  datastore:
    fs: 
      path:
        upload:  "/Users/kami/work/dev/portfolio/tasks/image-upload-service/upload"
        preview: "/Users/kami/work/dev/portfolio/tasks/image-upload-service/preview"

#      enabled: true
#    s3:
#      enabled: false
#    mongodb:
#      enabled: false
      
cond:
  bean: bean2
